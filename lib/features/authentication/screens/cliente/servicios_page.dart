import 'package:flutter/material.dart';

class ServiciosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Estás en la página de Servicios', style: TextStyle(fontFamily: 'Mont-Bold')),
      ),
    );
  }
}
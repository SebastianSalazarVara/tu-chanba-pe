import 'package:flutter/material.dart';

class PerfilPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Estás en la página de Perfil', style: TextStyle(fontFamily: 'Mont-Bold')),
      ),
    );
  }
}

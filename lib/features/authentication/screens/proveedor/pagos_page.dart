import 'package:flutter/material.dart';

class PagosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Estás en la página de Pagos', style: TextStyle(fontFamily: 'Mont-Bold')),
      ),
    );
  }
}

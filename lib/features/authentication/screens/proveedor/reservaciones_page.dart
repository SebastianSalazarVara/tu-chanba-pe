import 'package:flutter/material.dart';

class ReservacionesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Estás en la página de Reservaciones', style: TextStyle(fontFamily: 'Mont-Bold')),
      ),
    );
  }
}
